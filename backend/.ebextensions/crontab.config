files:
  "/tmp/crontab_config":
     mode: "000644"
     owner: root
     group: root
     content: |
        # Auto-renew let's encrypt SSL certificates (check every hour)
        0     *     *     *     *      sudo certbot renew

